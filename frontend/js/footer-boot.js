// ============================================================
// FOOTER BOOTSTRAP
// Tiny entry module used by pages that only need to start the shared footer.
// ============================================================

import { renderFooter } from "./components/footer.js";

// Render the reusable footer into the page's #site-footer placeholder.
renderFooter();
