import { resetPassword } from "../services/auth-service.js";
const token=new URLSearchParams(location.search).get("token")||"";const form=document.getElementById("reset-form"),message=document.getElementById("reset-message");
if(!token){message.textContent="This reset link is missing its security token.";message.className="form-message form-message-error";form.querySelector("button").disabled=true;}
form?.addEventListener("submit",async(e)=>{e.preventDefault();const p=document.getElementById("reset-password").value,c=document.getElementById("reset-confirm").value;if(p!==c){message.textContent="Passwords do not match.";message.className="form-message form-message-error";return;}const b=form.querySelector("button");b.disabled=true;const result=await resetPassword(token,p);message.textContent=result.message;message.className=`form-message ${result.success?"form-message-success":"form-message-error"}`;if(result.success)setTimeout(()=>location.replace("./login.html"),1200);else b.disabled=false;});
